<%= form_with(model: proceeding, local: true) do |form| %>
  <% if proceeding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proceeding.errors.count, "error") %> prohibited this proceeding from being saved:</h2>

      <ul>
      <% proceeding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :patient_id %>
    <%= form.text_field :patient_id %>
  </div>

  <div class="field">
    <%= form.label :medication_id %>
    <%= form.text_field :medication_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
